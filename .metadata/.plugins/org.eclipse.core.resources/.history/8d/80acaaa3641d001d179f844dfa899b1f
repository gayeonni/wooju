<html layout:decorate="~{layout}">

<div layout:fragment="content" class="container my-3">
	<div class="row my-3">
		<div class="col-6">
			<a th:href="@{/sale/create}" class="btn btn-primary"
				style="font-size: 20px">상품 등록하기</a>
		</div>
		<p>
		<div class="container">
			<div class="card">
				<div class="card-body">
					<div class="row">

						<div style="font-size: 30px">Would you use it? 여성 안심 중고 거래
							플랫폼 우주마켓</div>
						<p>
						<div class="col-3" th:each="question, loop: ${paging}">
							<a th:href="@{|/sale/detail/${sale.id}|}"> <img
								class="card-img-top" th:src="@{/img/main_logo_rm.png}" alt=""
								style="width: 100%"></a>
							<h5 class="card-title">
								<div th:text="${sale.subject}"></div>
							</h5>
							<h7 class="card-text">
							<div th:if="${sale.author != null}"
								th:text="${sale.author.username}"></div>
							</h7>
							<p class="card-text">
							<div th:if="${sale.category != null}"
								th:text="${sale.category.name}"></div>
							</p>
						</div>
					</div>
				</div>
			</div>
		</div>

	</div>

	<!-- 페이징처리 시작 -->
    <div th:if="${!paging.isEmpty()}">
        <ul class="pagination justify-content-center">
            <li class="page-item" th:classappend="${!paging.hasPrevious} ? 'disabled'">
                <a class="page-link"
                    th:href="@{|?page=${paging.number-1}|}">
                    <span>이전</span>
                </a>
            </li>
            <li th:each="page: ${#numbers.sequence(0, paging.totalPages-1)}"
                th:classappend="${page == paging.number} ? 'active'" 
                class="page-item">
                <a th:text="${page}" class="page-link" th:href="@{|?page=${page}|}"></a>
            </li>
            <li class="page-item" th:classappend="${!paging.hasNext} ? 'disabled'">
                <a class="page-link" th:href="@{|?page=${paging.number+1}|}">
                    <span>다음</span>
                </a>
            </li>
        </ul>
    </div>

</div>
	</script>
</html>